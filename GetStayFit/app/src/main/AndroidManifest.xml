<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.farshad.finalproject">

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InflexibilityActivity" />
        <activity android:name=".MaintainActivity" />
        <activity android:name=".HomeWrokoutActivity" />
        <activity android:name=".StartExerciseActivity" />
        <activity android:name=".StartExercise2Activity" />
        <activity android:name=".StartExercise3Activity" />
        <activity android:name=".ImageViewActivity" />
        <activity android:name=".ResultsActivity"></activity>
    </application>

</manifest>